---
phase: 01-product-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - webapp/app/product/[id]/page.tsx
  - webapp/app/globals.css
autonomous: false
requirements:
  - PROD-01
  - PROD-03

must_haves:
  truths:
    - "Product page shows a 'Compare Prices' section when matches are found from other stores"
    - "Each vendor card shows product name, store name, price, rating, and stock status"
    - "The cheapest listing displays a 'Best Value' badge (accent-primary color, #CCFF00)"
    - "The 'Compare Prices' section is hidden when fewer than 2 matches are found (single-vendor only)"
    - "Each vendor card links to the external store (opens in new tab)"
    - "The current product is NOT duplicated in the comparison section"
  artifacts:
    - path: "webapp/app/product/[id]/page.tsx"
      provides: "Multi-vendor comparison section with VendorCard components"
      contains: "Compare Prices"
    - path: "webapp/app/globals.css"
      provides: "Vendor card styling (.vendor-card class)"
      contains: ".vendor-card"
  key_links:
    - from: "webapp/app/product/[id]/page.tsx"
      to: "GET /api/search/matches"
      via: "fetch in useEffect when product URL and store are available"
      pattern: "api/search/matches"
    - from: "VendorCard (first item)"
      to: "badge-best CSS class"
      via: "conditional className when isBestValue is true"
      pattern: "badge-best"
---

<objective>
Add a multi-vendor "Compare Prices" section to the product detail page that shows the same product from other stores, with the cheapest listing marked as "Best Value".

Purpose: The current product page only shows one store's listing. Users cannot compare prices across stores without going back to search. This section shows side-by-side vendor cards so users can instantly see which store has the best price.

Output: Product page with multi-vendor comparison section and Best Value badge.
</objective>

<execution_context>
@/Users/ahad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-product-experience/01-RESEARCH.md
@.planning/phases/01-product-experience/01-02-SUMMARY.md

@webapp/app/product/[id]/page.tsx
@webapp/app/globals.css
@backend/src/routes/search.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-vendor comparison section to product page</name>
  <files>
    webapp/app/product/[id]/page.tsx
    webapp/app/globals.css
  </files>
  <action>
    Modify `webapp/app/product/[id]/page.tsx` to add a "Compare Prices" section.

    **State additions:**
    Add two new state variables inside `ProductDetail`:
    ```typescript
    const [matchedProducts, setMatchedProducts] = useState<any[]>([]);
    const [matchesLoading, setMatchesLoading] = useState(false);
    ```

    **New useEffect for fetching matches:**
    Add a useEffect that runs when `scrapedProduct` has a `url` and `store`:
    ```typescript
    useEffect(() => {
      if (!scrapedProduct?.url || !scrapedProduct?.store) return;
      setMatchesLoading(true);
      fetch(`${API_BASE}/search/matches?url=${encodeURIComponent(scrapedProduct.url)}&store=${encodeURIComponent(scrapedProduct.store)}`)
        .then(res => res.ok ? res.json() : null)
        .then(data => {
          if (data?.matches) {
            setMatchedProducts(data.matches);
          }
        })
        .catch(() => {})
        .finally(() => setMatchesLoading(false));
    }, [scrapedProduct?.url, scrapedProduct?.store]);
    ```

    **VendorCard component (define above ProductDetail):**
    ```tsx
    function VendorCard({ product, isBestValue }: { product: any; isBestValue: boolean }) {
      const price = typeof product.price === 'number' ? product.price : parseInt(String(product.price || '0').replace(/[^\d]/g, ''), 10) || 0;
      return (
        <div style={{ position: 'relative', padding: '20px', background: 'var(--bg-surface)', borderRadius: '16px', border: `1px solid ${isBestValue ? 'var(--accent-primary)' : 'var(--border-light)'}`, transition: 'border-color 0.2s' }}>
          {isBestValue && (
            <span className="badge badge-best" style={{ position: 'absolute', top: '-8px', left: '16px', fontSize: '11px', fontWeight: 700, padding: '4px 10px', letterSpacing: '0.05em' }}>
              BEST VALUE
            </span>
          )}
          {/* Store badge */}
          <div style={{ display: 'flex', alignItems: 'center', gap: '8px', marginBottom: '12px' }}>
            <span style={{ fontSize: '11px', fontFamily: 'var(--font-display)', fontWeight: 700, textTransform: 'uppercase', letterSpacing: '0.08em', padding: '3px 8px', borderRadius: '4px', background: 'rgba(255,255,255,0.06)', border: '1px solid var(--border-light)', color: 'var(--text-secondary)' }}>
              {product.store}
            </span>
            {product.inStock === false && (
              <span style={{ fontSize: '11px', color: '#FF4444', fontWeight: 600 }}>Out of Stock</span>
            )}
          </div>
          {/* Product name */}
          <h4 style={{ fontSize: '14px', lineHeight: '1.4', marginBottom: '12px', color: 'var(--text-main)', display: '-webkit-box', WebkitLineClamp: 2, WebkitBoxOrient: 'vertical', overflow: 'hidden' }}>
            {product.name}
          </h4>
          {/* Price */}
          <div className="product-price" style={{ fontSize: '20px', marginBottom: '16px' }}>
            Rs. {price.toLocaleString('en-PK')}
          </div>
          {/* Rating */}
          {product.rating > 0 && (
            <div style={{ fontSize: '12px', color: 'var(--text-muted)', marginBottom: '16px' }}>
              {'★'.repeat(Math.round(product.rating))}{'☆'.repeat(5 - Math.round(product.rating))} {product.rating.toFixed(1)} ({product.reviewsCount || 0})
            </div>
          )}
          {/* Visit store link */}
          <a href={product.url} target="_blank" rel="noopener noreferrer" className="btn btn-secondary" style={{ width: '100%', display: 'flex', justifyContent: 'center', alignItems: 'center', gap: '8px', height: '40px', fontSize: '12px' }}>
            Visit {product.store}
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
          </a>
        </div>
      );
    }
    ```

    **Render the comparison section:**
    Insert the "Compare Prices" section AFTER the main product info section (after the closing `</div>` of the flex layout around line 403) and BEFORE the Description section. Place it at approximately the same level as Description:

    ```tsx
    {/* Multi-vendor Comparison */}
    {matchesLoading && (
      <div style={{ marginBottom: '60px', padding: '24px', textAlign: 'center', color: 'var(--text-muted)', fontSize: '13px' }}>
        Searching other stores...
      </div>
    )}
    {!matchesLoading && matchedProducts.length > 1 && (
      <div style={{ marginBottom: '60px' }}>
        <div className="section-title" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '20px' }}>
          <h3>Compare Prices</h3>
          <span style={{ fontSize: '13px', color: 'var(--text-muted)' }}>{matchedProducts.length} stores</span>
        </div>
        <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(280px, 1fr))', gap: '16px' }}>
          {matchedProducts.map((p, idx) => (
            <VendorCard key={p.url} product={p} isBestValue={idx === 0} />
          ))}
        </div>
      </div>
    )}
    ```

    Key details:
    - Only show when `matchedProducts.length > 1` (at least 1 match from another store, since the source is excluded)
    - `idx === 0` gets Best Value badge because matches are pre-sorted by price ascending from the backend
    - The section uses the existing `section-title` CSS class and `card` styling patterns
    - Each VendorCard links to the external store in a new tab

    **CSS additions in `webapp/app/globals.css`:**
    Add hover effect for vendor cards. The existing `.badge-best` class already provides the styling (accent-primary background, #CCFF00). No new badge CSS needed.

    Add minimal CSS for the vendor card hover:
    ```css
    /* Vendor comparison cards */
    .vendor-card:hover {
      border-color: rgba(204, 255, 0, 0.3) !important;
    }
    ```
    Add this in the CARDS section of globals.css (around line 518, after the `a.card:hover` rule).
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/webapp && npx tsc --noEmit --pretty 2>&amp;1 | head -30
    </automated>
  </verify>
  <done>
    Product page fetches matches from /api/search/matches and renders a "Compare Prices" grid section when multiple stores have the same product. Cheapest listing has "Best Value" badge. Section is hidden when fewer than 2 matches. Each card links to the external store.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Verify multi-vendor product page</name>
  <files>none</files>
  <action>
    Manual verification that the multi-vendor comparison section works correctly.
  </action>
  <verify>
    <manual>
      1. Start the backend: `cd backend && npm run dev`
      2. Start the webapp: `cd webapp && npm run dev`
      3. Go to http://localhost:3000/search and search for a popular product (e.g. "iPhone 16" or "Galaxy S24")
      4. Click on any product result to open the product detail page
      5. Wait for the "Searching other stores..." text to resolve
      6. Verify:
         a. A "Compare Prices" section appears with store count
         b. Multiple vendor cards are shown in a grid
         c. The cheapest listing has a "BEST VALUE" badge (yellow/green accent)
         d. Each card shows store name, product name, price, and rating
         e. "Visit [Store]" button opens the external store in a new tab
         f. The current product is NOT duplicated in the comparison section
      7. Test with a niche product that likely only exists on one store -- verify the "Compare Prices" section does NOT appear (no matches)
    </manual>
  </verify>
  <done>
    Multi-vendor comparison section renders correctly with Best Value badge on cheapest listing, and gracefully hides when no cross-store matches exist.
  </done>
</task>

</tasks>

<verification>
1. Webapp TypeScript compiles: `cd webapp && npx tsc --noEmit`
2. "Compare Prices" section appears on product pages with cross-store matches
3. "BEST VALUE" badge appears on cheapest vendor card
4. Section is hidden when fewer than 2 matches found
5. Each vendor card links to the correct external store
6. Current product URL is not duplicated in matches
7. Hover effect on vendor cards works
</verification>

<success_criteria>
- [ ] Product page shows "Compare Prices" grid with vendor cards when matches exist
- [ ] Each vendor card displays store name, product name, price, rating
- [ ] "BEST VALUE" badge on cheapest listing (badge-best CSS class)
- [ ] Comparison section hidden when fewer than 2 matches
- [ ] Visit Store links open external store in new tab
- [ ] Human verified the feature works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-product-experience/01-03-SUMMARY.md`
</output>
