---
phase: 02-price-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/db.service.ts
  - backend/src/routes/search.routes.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "Backend persists daily price snapshots into Supabase (one row per product_url+store+day)"
    - "Every time products are scraped (search and product detail), the backend attempts to record snapshots"
    - "Snapshot recording is idempotent (does not create duplicates when called multiple times same day)"
  artifacts:
    - path: "backend/src/services/db.service.ts"
      provides: "db.recordPriceSnapshots + db.recordPriceSnapshot helpers using Supabase upsert"
      contains: "recordPriceSnapshot"
    - path: "backend/src/routes/search.routes.ts"
      provides: "Snapshot recording after scrape (search results and product detail scrape)"
      contains: "recordPriceSnapshot"
  key_links:
    - from: "POST /api/search"
      to: "db.recordPriceSnapshots"
      via: "after rankProducts()"
      pattern: "recordPriceSnapshots"
    - from: "GET /api/search/product"
      to: "db.recordPriceSnapshot"
      via: "after scrapeProductDetail()"
      pattern: "recordPriceSnapshot"
---

<objective>
Add backend support for daily price snapshot persistence in Supabase and wire it into the scraper pipeline (search + product detail scraping).

Purpose: Phase 2 requires real historical prices. The backend must store snapshots whenever it scrapes products, and the storage must be idempotent and daily-bucketed.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/services/db.service.ts
@backend/src/routes/search.routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add snapshot persistence helpers to db.service</name>
  <files>
    backend/src/services/db.service.ts
  </files>
  <action>
    Add a new "Price History" section to `backend/src/services/db.service.ts` that provides:

    1) `recordPriceSnapshot(input)` for a single row
    2) `recordPriceSnapshots(inputs)` for batch upsert
    3) `getPriceHistoryByUrl(productUrl)` (added in plan 02-02, but you can stub it here if convenient)

    Data model (Supabase table name should be `price_history`):
    - `product_url` (text, required)
    - `store` (text, required)
    - `price` (numeric/int, required)
    - `day` (date, required) - derived from `new Date().toISOString().slice(0, 10)`
    - `scraped_at` (timestamp, optional but recommended)

    Upsert requirements:
    - Must be idempotent per day: use `upsert(..., { onConflict: 'product_url,store,day' })`
    - If the same day row already exists, update `price` and `scraped_at` (keep latest)

    NOTE: The SQL migration itself (creating the table + unique constraint) will be handled outside code. In code, assume the table exists.
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/backend && npm run build
    </automated>
  </verify>
  <done>
    db.service exposes price history snapshot upsert helpers with daily idempotency via onConflict.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record snapshots on search scrape (POST /api/search)</name>
  <files>
    backend/src/routes/search.routes.ts
  </files>
  <action>
    In `backend/src/routes/search.routes.ts` inside `router.post('/')`:

    After ranking is computed, batch upsert snapshots for ALL ranked results (or at least the first 50 if you need to cap load).

    Each snapshot should include:
    - product_url: product.url
    - store: product.store
    - price: product.price
    - day: today (YYYY-MM-DD)
    - scraped_at: now

    This must be best-effort and must NOT fail the request:
    - Wrap in try/catch and only log errors
    - Do not block responding if snapshots fail
  </action>
  <verify>
    <manual>
      Run backend, trigger a search, and confirm the `price_history` table receives rows for that day.
    </manual>
  </verify>
  <done>
    Every search scrape attempts to persist daily price snapshots.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record snapshots on product detail scrape (GET /api/search/product)</name>
  <files>
    backend/src/routes/search.routes.ts
  </files>
  <action>
    In `backend/src/routes/search.routes.ts` inside `router.get('/product')`:

    After `scrapeProductDetail()` returns `detail`, record a snapshot for the single product:
    - product_url: request query `url`
    - store: request query `store`
    - price: `detail.price`
    - day/scraped_at: now

    Make it best-effort (no request failure if snapshot insert fails).
  </action>
  <verify>
    <manual>
      Open a product page (which calls /api/search/product) and verify a snapshot is stored for that URL+store+day.
    </manual>
  </verify>
  <done>
    Product detail scrapes also store daily snapshots.
  </done>
</task>

</tasks>

<verification>
1. Backend compiles (`npm -C backend run build`)
2. POST /api/search records snapshots into `price_history`
3. GET /api/search/product records snapshots into `price_history`
4. Repeated scrapes on the same day do not create duplicate rows for the same product_url+store+day
</verification>

<success_criteria>
- [ ] Price history snapshots are persisted daily in Supabase
- [ ] Search + product detail scrapes both attempt snapshot recording
- [ ] Snapshot recording is idempotent for same-day repeats
</success_criteria>

