---
phase: 02-price-history
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/src/services/db.service.ts
  - backend/src/routes/history.routes.ts
  - backend/src/server.ts
autonomous: true
requirements:
  - HIST-03

must_haves:
  truths:
    - "Backend exposes an API endpoint to fetch price history points for a product URL (optionally filtered by store)"
    - "API returns time-ordered points usable directly by the chart UI"
  artifacts:
    - path: "backend/src/routes/history.routes.ts"
      provides: "GET /api/history endpoint"
      contains: "router.get"
    - path: "backend/src/services/db.service.ts"
      provides: "db.getPriceHistory(...) query against price_history table"
      contains: "getPriceHistory"
    - path: "backend/src/server.ts"
      provides: "Mount historyRoutes under /api/history"
      contains: "historyRoutes"
  key_links:
    - from: "GET /api/history"
      to: "db.getPriceHistory"
      via: "query params url + optional store"
      pattern: "getPriceHistory"
---

<objective>
Create a backend API endpoint for fetching stored price history for a product URL so the web/mobile charts can render real data.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/services/db.service.ts
@backend/src/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add db query for price history</name>
  <files>
    backend/src/services/db.service.ts
  </files>
  <action>
    Add `db.getPriceHistory(productUrl: string, store?: string | null)` that queries the `price_history` table:
    - filter by `product_url = productUrl`
    - if store provided, also filter by `store = store`
    - order by `day` ascending

    Response should be raw rows; the route will shape the API response.
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/backend && npm run build
    </automated>
  </verify>
  <done>
    db.getPriceHistory exists and queries price_history in a deterministic order.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GET /api/history route</name>
  <files>
    backend/src/routes/history.routes.ts
    backend/src/server.ts
  </files>
  <action>
    Create a new route file `backend/src/routes/history.routes.ts` exporting `historyRoutes`.

    Add endpoint:
    - `GET /api/history?url=...&store=...`
    - `url` is required
    - `store` is optional

    Response shape:
    ```json
    {
      "url": "...",
      "store": "daraz|telemart|shophive|null",
      "points": [
        { "day": "YYYY-MM-DD", "price": 123456, "store": "daraz" }
      ]
    }
    ```

    Notes:
    - `points` must be ordered by day ASC
    - For now, do not require auth (price history is not user-private)
    - Convert snake_case to camelCase where relevant (day/price/store are already simple)
  </action>
  <verify>
    <manual>
      curl "http://localhost:3001/api/history?url=...&store=daraz" and confirm it returns ordered points.
    </manual>
  </verify>
  <done>
    GET /api/history exists and returns usable price history points for a product URL.
  </done>
</task>

</tasks>

<verification>
1. Backend compiles (`npm -C backend run build`)
2. GET /api/history returns points ordered by day ASC
3. Works both with and without store filter
</verification>

<success_criteria>
- [ ] Price history fetch endpoint exists at GET /api/history
- [ ] Endpoint returns stable JSON for chart rendering
</success_criteria>

