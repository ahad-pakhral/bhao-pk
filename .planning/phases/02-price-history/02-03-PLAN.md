---
phase: 02-price-history
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - webapp/app/product/[id]/page.tsx
  - webapp/components/PriceHistoryChart.tsx
  - mobile/src/screens/ProductDetailScreen.tsx
  - mobile/src/components/PriceHistoryChart.tsx
autonomous: true
requirements:
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "Web product page shows a price history chart based on real stored points (not placeholder)"
    - "Mobile product detail page shows a price history chart based on real stored points (not dummy data)"
  artifacts:
    - path: "webapp/components/PriceHistoryChart.tsx"
      provides: "Chart component accepts real points and renders them"
      contains: "PriceHistoryChart"
    - path: "webapp/app/product/[id]/page.tsx"
      provides: "Fetches GET /api/history with product url/store and passes points to chart"
      contains: "/api/history"
    - path: "mobile/src/screens/ProductDetailScreen.tsx"
      provides: "Fetches GET /api/history and passes points to mobile chart"
      contains: "/api/history"
  key_links:
    - from: "webapp/app/product/[id]/page.tsx"
      to: "GET /api/history"
      via: "url + store from product"
      pattern: "api/history"
    - from: "mobile/src/screens/ProductDetailScreen.tsx"
      to: "GET /api/history"
      via: "url + store from product"
      pattern: "api/history"
---

<objective>
Replace mock/placeholder price history charts on web + mobile product pages with real data from GET /api/history.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@webapp/app/product/[id]/page.tsx
@webapp/components/PriceHistoryChart.tsx
@mobile/src/screens/ProductDetailScreen.tsx
@mobile/src/components/PriceHistoryChart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web product page uses real price history points</name>
  <files>
    webapp/app/product/[id]/page.tsx
    webapp/components/PriceHistoryChart.tsx
  </files>
  <action>
    Update the web product detail page to fetch real history:
    - When `scrapedProduct.url` and `scrapedProduct.store` are known, call:
      `GET ${API_BASE}/history?url=${encodeURIComponent(url)}&store=${encodeURIComponent(store)}`
    - Store the returned `points` in state and pass to `PriceHistoryChart`

    Update `PriceHistoryChart` (if needed) to accept points shaped as:
    `{ day: string; price: number; store: string }[]`

    Remove the local "Generate price history" placeholder logic once real points render.
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/webapp && npm run build
    </automated>
  </verify>
  <done>
    Web product page renders chart from stored price history points.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mobile product page uses real price history points</name>
  <files>
    mobile/src/screens/ProductDetailScreen.tsx
    mobile/src/components/PriceHistoryChart.tsx
  </files>
  <action>
    Update the mobile product detail screen to fetch from the same endpoint:
    - Use the product url/store available in screen params/product object
    - Call `GET ${API_BASE}/history?url=...&store=...`
    - Pass points into the mobile `PriceHistoryChart` component

    Remove any remaining dummy/mock price history source for this screen.
  </action>
  <verify>
    <manual>
      Run mobile app, open a product detail page, and confirm the chart updates based on backend points.
    </manual>
  </verify>
  <done>
    Mobile product page renders chart from stored price history points.
  </done>
</task>

</tasks>

<verification>
1. Webapp builds (`npm -C webapp run build`)
2. Web product page calls GET /api/history and charts real points
3. Mobile product page calls GET /api/history and charts real points (no dummy data)
</verification>

<success_criteria>
- [ ] Web price history chart is real data
- [ ] Mobile price history chart is real data
</success_criteria>

