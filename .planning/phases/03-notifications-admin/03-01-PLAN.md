---
phase: 03-notifications-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/email.service.ts
  - backend/src/services/alert-checker.service.ts
  - backend/package.json
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02

must_haves:
  truths:
    - "When an alert triggers (price <= target), backend sends an email to the alert owner"
    - "Email includes product name, old price, new price, and a link back to the Bhao product page"
    - "If email provider env vars are missing, alert checker logs a clear warning and does not mark the alert notified"
  artifacts:
    - path: "backend/src/services/email.service.ts"
      provides: "sendPriceDropEmail(to, payload) implementation"
      contains: "sendPriceDropEmail"
    - path: "backend/src/services/alert-checker.service.ts"
      provides: "alert checker sends email on trigger"
      contains: "sendPriceDropEmail"
  key_links:
    - from: "Alert Checker"
      to: "sendPriceDropEmail"
      via: "triggered alert (price <= target)"
      pattern: "sendPriceDropEmail"
---

<objective>
Add email notifications on price-drop alert triggers, including product context and a link back to the Bhao product page.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/services/alert-checker.service.ts
@backend/src/services/db.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add email service abstraction</name>
  <files>
    backend/src/services/email.service.ts
    backend/package.json
  </files>
  <action>
    Create a simple email service abstraction in `backend/src/services/email.service.ts`:
    - Implement `sendPriceDropEmail(to: string, data: { productName: string; productUrl: string; oldPrice?: number; newPrice: number; targetPrice: number; store: string })`
    - Use a provider (Resend recommended) via REST API or npm package
    - Read env vars for configuration (e.g., RESEND_API_KEY + EMAIL_FROM)
    - If missing, throw a clear error ("Email provider not configured")
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/backend && npm run build
    </automated>
  </verify>
  <done>
    Email service exists and can be called from alert checker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate email sending into alert checker</name>
  <files>
    backend/src/services/alert-checker.service.ts
  </files>
  <action>
    Update `backend/src/services/alert-checker.service.ts` so that when an alert triggers:
    - Fetch the user's email (via Supabase auth user or profile lookup strategy)
    - Determine product name (from scrape result)
    - Determine old price (best-effort from latest previous `price_history` point for that URL/store)
    - Send an email with productName, oldPrice, newPrice, targetPrice, and a link to the Bhao product page
      (Bhao link should be `/product/${encodeURIComponent(productUrl)}?url=...&store=...`)
    - Only mark the alert as notified if the email send succeeds
  </action>
  <verify>
    <manual>
      Configure email provider env vars, create an alert with a low target, run the checker, and confirm an email arrives with the required fields.
    </manual>
  </verify>
  <done>
    Triggered alerts send email notifications with product context and a Bhao link.
  </done>
</task>

</tasks>

<verification>
1. Backend compiles (`npm -C backend run build`)
2. Alert checker sends email on trigger (manual verification)
3. Email contains product name, old price, new price, and Bhao link
</verification>

