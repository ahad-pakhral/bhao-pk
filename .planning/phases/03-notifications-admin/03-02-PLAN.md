---
phase: 03-notifications-admin
plan: 02
type: execute
wave: 1
depends_on:
  - 03-01
files_modified:
  - backend/supabase-migration.sql
  - backend/src/services/db.service.ts
  - backend/src/services/alert-checker.service.ts
autonomous: true
requirements:
  - NOTF-03

must_haves:
  truths:
    - "Backend prevents duplicate emails for the same alert trigger"
    - "Notification send is tracked in DB (timestamp + last_notified_price or similar)"
  artifacts:
    - path: "backend/supabase-migration.sql"
      provides: "alert notification tracking columns"
      contains: "notification"
    - path: "backend/src/services/db.service.ts"
      provides: "db.updateAlertNotificationState(...) helper"
      contains: "updateAlert"
  key_links:
    - from: "Alert Checker"
      to: "db.updateAlert(...)"
      via: "after successful email send"
      pattern: "updateAlert"
---

<objective>
Add explicit DB tracking so the backend never sends duplicate emails for the same alert trigger.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/supabase-migration.sql
@backend/src/services/alert-checker.service.ts
@backend/src/services/db.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notification tracking columns to price_alerts</name>
  <files>
    backend/supabase-migration.sql
  </files>
  <action>
    Extend `price_alerts` with:
    - `notified_at` timestamptz nullable
    - `last_notified_price` real nullable
    Use `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` and keep the migration re-runnable.
  </action>
  <verify>
    <manual>
      Run SQL in Supabase and confirm columns exist in `price_alerts`.
    </manual>
  </verify>
  <done>
    DB schema supports duplicate-prevention tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update alert checker duplicate prevention logic</name>
  <files>
    backend/src/services/alert-checker.service.ts
    backend/src/services/db.service.ts
  </files>
  <action>
    Update the checker so:
    - If `is_notified` is already true OR `notified_at` is set, do not send another email
    - After sending, set `is_notified=true`, `notified_at=now()`, and `last_notified_price=currentPrice`
  </action>
  <verify>
    <manual>
      Force-run the checker twice and confirm the second run does not re-send.
    </manual>
  </verify>
  <done>
    Alerts never send duplicate emails after a successful send.
  </done>
</task>

</tasks>

