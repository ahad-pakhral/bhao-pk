---
phase: 03-notifications-admin
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - backend/src/middleware/admin.middleware.ts
  - backend/src/routes/admin.routes.ts
  - backend/src/server.ts
  - backend/src/services/db.service.ts
autonomous: true
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "Only ADMIN users can access admin stats endpoint"
    - "Backend provides real stats: users, active alerts, searches today, wishlist items"
  artifacts:
    - path: "backend/src/routes/admin.routes.ts"
      provides: "GET /api/admin/stats"
      contains: "stats"
    - path: "backend/src/middleware/admin.middleware.ts"
      provides: "requireAdmin middleware"
      contains: "requireAdmin"
  key_links:
    - from: "GET /api/admin/stats"
      to: "db.getUserStats or new db.adminStats"
      via: "service role Supabase queries"
      pattern: "getUserStats"
---

<objective>
Implement role-based admin auth and a real backend stats endpoint for the admin dashboard UI.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/server.ts
@backend/src/services/db.service.ts
@backend/src/middleware/auth.middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requireAdmin middleware</name>
  <files>
    backend/src/middleware/admin.middleware.ts
    backend/src/services/db.service.ts
  </files>
  <action>
    Add `requireAdmin` middleware that:
    - assumes `requireAuth` already attached `req.user` (Supabase auth user)
    - loads the profile row from `users` table via db.findUser(req.user.id)
    - checks `role === 'ADMIN'`
    - returns 403 if not admin
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/backend && npm run build
    </automated>
  </verify>
  <done>
    Non-admin users cannot access admin endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add GET /api/admin/stats endpoint</name>
  <files>
    backend/src/routes/admin.routes.ts
    backend/src/server.ts
    backend/src/services/db.service.ts
  </files>
  <action>
    Create `backend/src/routes/admin.routes.ts` with:
    - `router.use(requireAuth)`
    - `router.use(requireAdmin)`
    - `GET /stats` returning:
      - total users count
      - active alerts count (is_notified=false)
      - searches today count
      - wishlist items count
    Mount it at `/api/admin` in `backend/src/server.ts`.
  </action>
  <verify>
    <manual>
      Call GET /api/admin/stats with a non-admin token (expect 403), then with an admin token (expect 200 + stats).
    </manual>
  </verify>
  <done>
    Admin stats endpoint returns real counts and is protected by role.
  </done>
</task>

</tasks>
