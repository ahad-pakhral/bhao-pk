---
phase: 03-notifications-admin
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - webapp/app/admin/dashboard/page.tsx
  - webapp/app/admin/login/page.tsx
  - webapp/store/authStore.ts
autonomous: true
requirements:
  - ADMN-02
  - ADMN-03

must_haves:
  truths:
    - "Admin dashboard shows real stats from GET /api/admin/stats"
    - "Non-admin users cannot access the dashboard page"
  artifacts:
    - path: "webapp/app/admin/dashboard/page.tsx"
      provides: "Dashboard fetches stats + renders real values"
      contains: "/api/admin/stats"
    - path: "webapp/app/admin/login/page.tsx"
      provides: "Admin login flow and redirect"
      contains: "admin"
---

<objective>
Wire the admin dashboard UI to the real backend stats endpoint and enforce admin-only access.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@webapp/app/admin/dashboard/page.tsx
@webapp/store/authStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protect admin pages</name>
  <files>
    webapp/app/admin/dashboard/page.tsx
    webapp/app/admin/login/page.tsx
    webapp/store/authStore.ts
  </files>
  <action>
    Ensure admin pages require authenticated user with role ADMIN:
    - If unauthenticated, redirect to /admin/login
    - If authenticated but role != ADMIN, redirect to / (or show 403)
  </action>
  <verify>
    <manual>
      Log in as a normal user and try /admin/dashboard (blocked). Log in as ADMIN and verify access.
    </manual>
  </verify>
  <done>
    Only admins can reach the admin dashboard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fetch and render real stats</name>
  <files>
    webapp/app/admin/dashboard/page.tsx
  </files>
  <action>
    Replace hardcoded `stats` values with a fetch call to:
    - `GET ${API_BASE}/admin/stats` with Bearer token
    Render counts for:
    - Total users
    - Active alerts
    - Searches today
    - Wishlist items
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/webapp && npm run build
    </automated>
  </verify>
  <done>
    Admin dashboard renders real backend stats.
  </done>
</task>

</tasks>

