---
phase: 04-mobile-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/supabase-migration.sql
  - backend/src/routes/recent.routes.ts
  - backend/src/server.ts
  - backend/src/services/db.service.ts
  - mobile/src/screens/HomeScreen.tsx
  - mobile/src/services/api/product.service.ts
autonomous: true
requirements:
  - MOBL-01
  - MOBL-02

must_haves:
  truths:
    - "Mobile Home shows real trending products from backend (no dummy TRENDING_PRODUCTS)"
    - "Mobile Recently Viewed is user-specific and persisted on backend (no dummy RECENTLY_VIEWED)"
    - "Opening a product on mobile records a recently viewed entry on backend"
  artifacts:
    - path: "backend/src/routes/recent.routes.ts"
      provides: "GET/POST /api/recently-viewed endpoints"
      contains: "recently-viewed"
    - path: "mobile/src/screens/HomeScreen.tsx"
      provides: "HomeScreen loads trending + recently viewed from API"
      contains: "getTrending"
  key_links:
    - from: "Mobile HomeScreen"
      to: "GET /api/search/trending"
      via: "productService.getTrending()"
      pattern: "/search/trending"
    - from: "Mobile ProductDetail open"
      to: "POST /api/recently-viewed"
      via: "record product view"
      pattern: "recently-viewed"
---

<objective>
Remove dummy home data from the mobile app and wire Home (trending + recently viewed) to real backend APIs.

This executes MOBL-01 and MOBL-02 and establishes the backend surface needed for user-specific recently viewed tracking.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/server.ts
@backend/src/routes/search.routes.ts

@mobile/src/screens/HomeScreen.tsx
@mobile/src/screens/ProductDetailScreen.tsx
@mobile/src/services/api/product.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recently_viewed storage + endpoints (backend)</name>
  <files>
    backend/supabase-migration.sql
    backend/src/services/db.service.ts
    backend/src/routes/recent.routes.ts
    backend/src/server.ts
  </files>
  <action>
    Add a Supabase-backed recently viewed feature:

    1) Extend `backend/supabase-migration.sql` (re-runnable) with a new table:
       - `recently_viewed`:
         - id uuid pk (gen_random_uuid())
         - user_id uuid (fk users.id)
         - product_url text not null
         - store text not null
         - name text not null
         - image_url text
         - last_viewed_at timestamptz default now()
         - UNIQUE(user_id, product_url)
       - RLS: users can read/write only their rows

    2) Add db helpers in `backend/src/services/db.service.ts`:
       - `upsertRecentlyViewed(userId, { productUrl, store, name, imageUrl })` (idempotent upsert)
       - `getRecentlyViewed(userId, limit=20)` ordered by last_viewed_at desc

    3) Add `backend/src/routes/recent.routes.ts` with `requireAuth`:
       - `GET /api/recently-viewed` -> returns list
       - `POST /api/recently-viewed` -> upserts a view
       Input payload: { productUrl, store, name, imageUrl? }
       Output: { ok: true }

    4) Mount in `backend/src/server.ts` at `/api/recently-viewed`.
  </action>
  <verify>
    <manual>
      Call GET/POST with a valid Bearer token and confirm rows appear in Supabase.
    </manual>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/backend && npm run build
    </automated>
  </verify>
  <done>
    Backend supports recently viewed persistence and retrieval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mobile Home trending to /api/search/trending (mobile)</name>
  <files>
    mobile/src/services/api/product.service.ts
    mobile/src/screens/HomeScreen.tsx
  </files>
  <action>
    Replace dummy trending:
    - Update `productService.getTrending()` to call `GET /search/trending` (backend exists) and normalize to mobile product model.
    - Update HomeScreen to load trending from productService and render it.
    - Remove imports/usage of TRENDING_PRODUCTS for the Home trending section.
  </action>
  <verify>
    <manual>
      Launch mobile app; Home trending renders real products and links open ProductDetail.
    </manual>
  </verify>
  <done>
    Home trending is real (no dummy data).
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire mobile Recently Viewed to backend (mobile)</name>
  <files>
    mobile/src/screens/HomeScreen.tsx
    mobile/src/screens/ProductDetailScreen.tsx
  </files>
  <action>
    Replace dummy RECENTLY_VIEWED:
    - On HomeScreen, fetch `GET /api/recently-viewed` (auth required) and render that list.
    - When the user opens a product detail screen, POST a view record to `/api/recently-viewed` with productUrl/store/name/imageUrl.
    - If the user is not logged in, show an empty state or fall back to local-only list (but do not use hardcoded dummy products).
  </action>
  <verify>
    <manual>
      Open 2 products, return Home, and confirm Recently Viewed shows those items. Refresh app, confirm it persists.
    </manual>
  </verify>
  <done>
    Recently viewed is backend-persisted and user-specific.
  </done>
</task>

</tasks>

<verification>
1. Mobile Home shows trending + recently viewed from API (no dummy arrays)
2. Supabase table `recently_viewed` exists and is populated
3. Recently viewed persists across app restarts for the same user
</verification>

