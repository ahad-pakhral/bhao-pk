---
phase: 04-mobile-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - mobile/src/services/api/wishlist.service.ts
  - mobile/src/hooks/useWishlist.ts
  - mobile/src/screens/WishlistScreen.tsx
  - mobile/src/screens/ProductDetailScreen.tsx
autonomous: true
requirements:
  - MOBL-03

must_haves:
  truths:
    - "Mobile wishlist uses backend Supabase wishlist_items (same as web)"
    - "Adding/removing on mobile reflects on web and vice versa"
    - "No dummy wishlist items or local-only wishlist source remains"
  artifacts:
    - path: "mobile/src/services/api/wishlist.service.ts"
      provides: "Wishlist CRUD against /api/wishlist"
      contains: "/wishlist"
  key_links:
    - from: "Mobile wishlist add"
      to: "POST /api/wishlist"
      via: "wishlistService.addToWishlist(payload)"
      pattern: "/wishlist"
---

<objective>
Wire the mobile wishlist to the real backend wishlist endpoints and remove any dummy/local-only wishlist behaviors.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/routes/wishlist.routes.ts
@webapp/hooks/useWishlist.ts

@mobile/src/services/api/wishlist.service.ts
@mobile/src/hooks/useWishlist.ts
@mobile/src/screens/WishlistScreen.tsx
@mobile/src/screens/ProductDetailScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align mobile wishlist service with backend contract</name>
  <files>
    mobile/src/services/api/wishlist.service.ts
  </files>
  <action>
    Backend contract is `POST /api/wishlist` with body:
    `{ store, url, name, imageUrl }`
    and response `{ item }`.

    Update mobile wishlist.service.ts to:
    - `getWishlist()` -> GET /wishlist returns `{ wishlist: WishlistItem[] }`
    - `addToWishlist(payload)` -> POST /wishlist with store/url/name/imageUrl (no productId)
    - `removeFromWishlist(id)` -> DELETE /wishlist/:id
    - Update types to reflect web/backend shape: id,url,name,store,imageUrl,createdAt,userId
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/mobile && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Mobile wishlist service matches backend.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mobile WishlistScreen to backend</name>
  <files>
    mobile/src/screens/WishlistScreen.tsx
    mobile/src/hooks/useWishlist.ts
  </files>
  <action>
    Remove dummy/local sources and ensure WishlistScreen uses the updated wishlist hook/service:
    - Load list from GET /wishlist (requires auth)
    - Remove uses DELETE /wishlist/:id and updates UI
    - Show meaningful empty state when list is empty
  </action>
  <verify>
    <manual>
      Add an item on web, open mobile wishlist, confirm it appears. Remove on mobile, confirm web updates after refresh.
    </manual>
  </verify>
  <done>
    Mobile wishlist is fully backend-synced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire product detail wishlist toggle</name>
  <files>
    mobile/src/screens/ProductDetailScreen.tsx
    mobile/src/hooks/useWishlist.ts
  </files>
  <action>
    Ensure ProductDetail screen toggles wishlist using the real backend id-based rows:
    - When adding, send store/url/name/imageUrl (requires real URL; block for dummy products)
    - When removing, remove by wishlist row id
  </action>
  <verify>
    <manual>
      Toggle wishlist from mobile product screen and confirm it persists + syncs to web.
    </manual>
  </verify>
  <done>
    Wishlist toggle works from product details.
  </done>
</task>

</tasks>

