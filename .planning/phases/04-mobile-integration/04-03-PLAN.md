---
phase: 04-mobile-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
files_modified:
  - mobile/src/services/api/alert.service.ts
  - mobile/src/hooks/useSmartAlerts.ts
  - mobile/src/screens/AlertsScreen.tsx
  - mobile/src/screens/ProductDetailScreen.tsx
autonomous: true
requirements:
  - MOBL-04

must_haves:
  truths:
    - "Mobile alerts list comes from backend (Supabase price_alerts) and matches web"
    - "Mobile can create alerts (target price) and see them appear on web"
    - "Mobile can delete alerts and see them removed on web"
    - "No dummy/local-only alerts data remains"
  artifacts:
    - path: "mobile/src/services/api/alert.service.ts"
      provides: "Alerts CRUD against /api/alerts and /api/alerts/enriched"
      contains: "/alerts"
  key_links:
    - from: "Mobile AlertsScreen"
      to: "GET /api/alerts/enriched"
      via: "useSmartAlerts.refresh"
      pattern: "/alerts/enriched"
---

<objective>
Wire the mobile alerts feature to the real backend alerts API (create/list/delete) and remove dummy/local-only alert behaviors.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

@backend/src/routes/alerts.routes.ts
@webapp/hooks/useSmartAlerts.ts

@mobile/src/services/api/alert.service.ts
@mobile/src/hooks/useSmartAlerts.ts
@mobile/src/screens/AlertsScreen.tsx
@mobile/src/screens/ProductDetailScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align mobile alert service with backend contract</name>
  <files>
    mobile/src/services/api/alert.service.ts
  </files>
  <action>
    Backend contract:
    - `GET /api/alerts` -> { alerts: [...] }
    - `GET /api/alerts/enriched` -> { alerts: [{...alert, product: {name,imageUrl,price,store} | null}] }
    - `POST /api/alerts` body: { targetPrice, productUrl } (or keyword)
    - `DELETE /api/alerts/:id`

    Update mobile alert.service.ts to:
    - Fetch enriched alerts for UI
    - Create alerts using productUrl + targetPrice (remove productId usage)
    - Delete by id
    - Remove unsupported toggle endpoint (backend doesn't implement PUT /alerts/:id)
  </action>
  <verify>
    <automated>
      cd /Users/ahad/Documents/Clawd/Bhao.pk/mobile && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    Mobile alert service matches backend.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire mobile AlertsScreen to enriched alerts</name>
  <files>
    mobile/src/hooks/useSmartAlerts.ts
    mobile/src/screens/AlertsScreen.tsx
  </files>
  <action>
    Use the enriched alerts list (like web):
    - Display product name/image/current price (if enrichment available)
    - Display target price
    - Delete removes from backend + local state
    - View product navigates to ProductDetail using productUrl/store (not dummy IDs)
  </action>
  <verify>
    <manual>
      Create an alert on web, open mobile alerts, confirm it appears with product context. Delete it on mobile and confirm web updates after refresh.
    </manual>
  </verify>
  <done>
    Mobile alerts are backend-synced and usable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire ProductDetail alert creation</name>
  <files>
    mobile/src/screens/ProductDetailScreen.tsx
  </files>
  <action>
    Ensure ProductDetail creates alerts via backend:
    - Use productUrl + targetPrice
    - Validate product has real url/store; block gracefully otherwise
  </action>
  <verify>
    <manual>
      Create an alert from mobile product page and confirm it appears in web alerts.
    </manual>
  </verify>
  <done>
    Alerts are creatable from mobile and sync across platforms.
  </done>
</task>

</tasks>

